"use client";

export default function DestinationCardButton({ title }) {
  return (
    <button
      onClick={() => {}}
      type="button"
      class="btn btn-primary"
      style={{
        padding: "2rem",
        fontSize: "24px",
        fontWeight: "400",
      }}
    >
      {title}
    </button>
  );
}
